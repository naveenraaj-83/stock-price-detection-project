# stock-price-detection-project
If you open the zipped project and wants to run it again, you can recreate the virtual environment instantly by opening a terminal in the folder and running:

# 1. Create a fresh virtual environment
python -m venv venv
# 2. Reinstall all libraries from requirements.txt
venv\Scripts\python -m pip install -r backend/requirements.txt
